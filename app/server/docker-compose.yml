services:
  postgres:
    image: postgres:16-alpine
    container_name: gapps-postgres
    environment:
      POSTGRES_DB: global_apps
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
